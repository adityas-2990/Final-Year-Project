# Copy this file to `.env` (in the same folder) and fill in your own key.
# `.env` is gitignored and must NEVER be committed.
#
# Create a key at: https://ollama.com  (Settings → API keys)
OLLAMA_API_KEY=your_api_key_here
